<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic to raphaaa.com is HTTPS-only (see usesCleartextTraffic="false"
  in the manifest). This just allows plain http:// to the Android emulator's alias
  for your machine's localhost (10.0.2.2), so you can point WEBSITE_URL at a local
  dev server (e.g. http://10.0.2.2:5173) while testing without editing the manifest.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
